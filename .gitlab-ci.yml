image: gradle

stages:
  - analysis

version-check:
  stage: analysis
  script:
    - java -version
    - gradle --version

test-coverage:
  stage: analysis
  script:
    - gradle jacocoTestReport
  artifacts:
    reports:
      junit: build/reports/jacoco/test/html/index.html